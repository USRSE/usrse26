# Tasks — <feature name>

Ordered, small tasks. Each names the story it serves and leaves the build
green (for this repo: `node scripts/build-program.js --file fixtures/schedule.csv`
succeeds and output diffs are as expected).

- [ ] 1. <task> (Story N)
- [ ] 2. <task> (Story N)
